# FruitNinja AI

Applying Vision ML techniques to the Fruit Ninja game with few labels.

## Execução do bot

Inferência padrão (sem debug/logs):

```bash
python -m src.bot.run
```

Tuning com diagnósticos (draw/debug/logs):

```bash
python -m src.bot.run --enable-diagnostics
```
